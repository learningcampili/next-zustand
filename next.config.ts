import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["res.cloudinary.com", "placehold.co"], // agrega aquí los dominios que necesitas
  },
};

export default nextConfig;
